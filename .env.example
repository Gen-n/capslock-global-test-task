# Copy to .env.local and fill in values

# Environment: local | dev | prod (default: local)
TEST_ENV=local

# Base URL override (default: http://localhost:5173)
BASE_URL=http://localhost:5173

# Required for dev/prod environments
DEV_EMAIL=
DEV_PASSWORD=
